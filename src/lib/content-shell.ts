/** Matches `SiteHeader` / hero horizontal rhythm — max width + responsive gutters. */
export const contentWidthShell =
  "mx-auto w-full max-w-[min(90rem,92vw)] px-4 sm:max-w-[min(90rem,88vw)] sm:px-6 md:px-10 lg:max-w-[min(90rem,82vw)] lg:px-12 xl:px-14";
